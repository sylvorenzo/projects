import { Button, Image, Modal, SafeAreaView, Text, TextInput, ToastAndroid, View,ActivityIndicator,StyleSheet} from 'react-native';
import { LinearGradient } from 'expo-linear-gradient';
import React,{useState} from 'react';
import { StatusBar } from 'expo-status-bar';
import { ScrollView, State, TouchableOpacity } from 'react-native-gesture-handler';




export default function BookingScreen()
{
 return(
     /*

  const styles = StyleSheet.create({
    container:{
        flex: 1,
        alignItems:'Center',
        justifyContent:'center'
    },
input:{
    borderWidth: 1,
    borderColor: '#777',
    padding: 8,
    margin: 10,
    width:200,

}
});
        
  return (
    <view className="App">
      <header className="App-header">
        <text><h1>DONATIONS</h1></text>
        
        <text>Change lives today by making a Donation</text>

        <label for="foundations">Choose a Foundation:</label>

<select name="foundations" id="foundations">
  <option value="Motsepe">Motsepe</option>
  <option value="Robinhood">Robinhood</option>
  <option value="Ramaphosa">Ramaphosa</option>
  <option value="Africa">Africa</option>
</select>
 <text>Founder Name:</text>

<View style={styles.container}>
<TextInput
keyboardType='default'
  style={styles.input}
  placeholder='e.g. John Gates '
  onChangeText={(val)=> (val)}/>

<text>Contacts: </text>
<TextInput
keyboardType='phone-pad'
  style={styles.input}
  placeholder='e.g. 0712345678'
  onChangeText={(val)=> (val)}/>



  

</View>


 <text>Comments</text>
 <TextInput
 multiline
 keyboardType='default'
  style={styles.input}
  placeholder='e.g. I think the donations will help'
  onChangeText={(val)=> (val)}/>





 <label for="amounts">Choose an Amount:</label>

<select name="amounts" id="amounts">
  <option value="R5">R5</option>
  <option value="R25">R25</option>
  <option value="R50">R50</option>
  <option value="R100">R100</option>
</select>
 <text>Enter amount:R</text>
<TextInput
keyboardType='numeric'
  style={styles.input}
  placeholder='e.g. 50'
  onChangeText={(val)=> (val)}/>

<label for="duration">Duration:</label>

<select name="duration" id="duration">
  <option value="Once-Off">Once-Off</option>
  <option value="Monthly">Monthly</option>
</select>

<br></br>
<Button
  title="Donate"
/>

      </header>
    </view>


   
  );
  
  };
     */
    <View >

        <Text> 
            Due to unfortune circumstances, the person who was supposed to do this page, has not fulfilled their promise.
            and because of that our project is incomplete. It is really sad, due to the fact that we had a tremendous amount 
            of time to complete this, unfortunately we can't control people and what they do with their time.
        </Text>
  </View>
 )
}